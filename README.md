# Algorithmic Information Criterion for Causal Inference

This project implements an algorithmic information criterion approach for determining causal direction between binary variables X and Y.

## Usage

```python
python work.py
```

The script simulates data with a known causal structure (X → Y) and uses encoding cost as a criterion to infer the causal direction.
